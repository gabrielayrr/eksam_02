
<article class="header-layout">
  <div class="logo">
    <a href="/">
      <img src="/images/logoheader.png" alt="logo" />
    </a>
  </div>
  
  <section class="link-content">
    <!-- Burger Menu Icon (Visible on mobile) -->
    <div class="burger-menu" onclick="toggleMenu()">
      <div class="bar"></div>
      <div class="bar"></div>
      <div class="bar"></div>
    </div>
    
    <!-- Navigation Menu -->
    <nav class="nav">
      <ul class="nav-list">
        <li><a href="/webshop">Webshop</a></li>
        <li><a href="/about">Om os</a></li>
        <li><a href="/kontakt">Kontakt</a></li>
      </ul>
    </nav>
    
    <div class="icons">
      <img src="/images/basket.png" class="basket" alt="" />
      <img src="/images/search.png" class="search" alt="" />
      <img src="/images/login.png" class="login" alt="" />
    </div>
  </section>
</article>

<script src="/scripts/burger.js" defer></script>

<style>
/* General Styles */
.basket{
    transition: transform 0.3s ease; /* Smooth transition for scaling */
    padding: 5px;
  }
  
  .basket:hover {
    transform: scale(1.2);
  }
  
  .search{
    transition: transform 0.3s ease; /* Smooth transition for scaling */
    padding: 5px;
  }
  
  .search:hover {
    transform: scale(1.2);
  }
  
  .login{
    transition: transform 0.3s ease; /* Smooth transition for scaling */
    padding: 5px;
  }
  
  .login:hover {
    transform: scale(1.2);
  }
  
  .logo{
    padding: 20px;
  }

  .icons img {
  width: 35px; /* Set a uniform width */
  height: 35px; /* Set a uniform height */
  object-fit: contain; /* Ensure the images maintain aspect ratio */
  display: inline-block; /* Keep them in a horizontal line */
  margin: 0 5px; /* Add spacing between the images */
  vertical-align: middle; /* Align the images to the middle of the line */
}


.header-layout {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 50px;

  position: relative; /* Required for absolute positioning of nav */

}

.logo img {
  height: 50px; /* Adjust size of the logo */
  object-fit: contain;
}

.link-content{
      display: flex;
      flex: left;
    }


/* Burger Menu Icon (Visible on mobile) */
.burger-menu {
  display: none; /* Hidden by default on desktop */
  flex-direction: column;
  justify-content: space-around;
  width: 30px;
  height: 25px;
  cursor: pointer;
  z-index: 1100; /* Ensure it's above the navigation */
  margin-right: 15px;
}

.burger-menu .bar {
  height: 4px;
  width: 100%;
  background-color: #C3E4DD;
  border-radius: 2px;
  transition: all 0.3s ease-in-out;
}

/* Burger Menu Animation */
.burger-menu.active .bar:nth-child(1) {
  transform: rotate(45deg) translate(5px, 5px);
}

.burger-menu.active .bar:nth-child(2) {
  opacity: 0; /* Hide the middle bar */
}

.burger-menu.active .bar:nth-child(3) {
  transform: rotate(-45deg) translate(5px, -5px);
}

/* Navigation Styles */
.nav {
  display: flex;
  list-style: none;
  gap: 40px;
}

.nav ul {
      display: flex;
      list-style: none;
      gap: 40px;
    }
.nav-list {
  display: flex;
  list-style: none;
  gap: 40px;
}

.nav a {
  text-decoration: none;
  color: #1e1e1e;
  font-size: 24px;
  font-family: fino-sans, sans-serif;
  font-style: normal;
  font-weight: 400;
}

.nav a:hover {
  text-decoration: underline;
}

/* Mobile Menu Styles */
@media (max-width: 768px) {
  /* Hide navigation menu by default */
  .link-content {
    display: flex;
    justify-content: space-between;
    width: 100%; /* Take full width */
  }

  .icons {
    order: 1; /* Set order so icons appear before the burger menu */
  }
  .nav {
    visibility: hidden;
    opacity: 0;
    position: absolute;
    top: 70px; /* Adjust based on header height */
    left: 0;
    width: 100%;
    background-color: #f5f4f1;
    flex-direction: column;
    align-items: center;
    transform: translateY(-100%);
    transition: all 0.3s ease-in-out;
    z-index: 1000;
  }

  /* Show the navigation when active */
  .nav.active {
    visibility: visible;
    opacity: 1;
    transform: translateY(0);
  }

  .nav-list {
    gap: 20px;
    padding: 20px 0;
  }

  /* Navigation links for mobile */
  .nav a {
    font-size: 18px;
    padding: 10px 0;
  }

  /* Show burger menu on mobile */
  .burger-menu {
    display: flex;
    order: 2;
  }
  
}






</style>
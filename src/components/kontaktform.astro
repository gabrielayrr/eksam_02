<section class="contact-form">
  <div class="kontakt-tekst">
    <p>
      Ønsker du at bestille en kage eller har spørgsmål til priser, bestilling
      mm., skal du sende en email til orders@bakemydaydk.com
    </p>
    <p>For generelle spørgsmål, kan du udfylde kontakt formularen her.</p>
  </div>
  <div class="kontaktform">
    <form action="#" method="POST">
      <label for="first-name">NAVN</label>
      <input type="text" id="first-name" name="first-name" required />

      <label for="email">EMAIL</label>
      <input type="email" id="email" name="email" required />

      <label for="last-name">EMNE</label>
      <input type="text" id="emne" name="emne" required />

      <label for="message"></label>
      <textarea
        id="message"
        name="message"
        placeholder="Skriv din besked her"
        rows="5"
        required></textarea>

      <button type="submit">SEND</button>
    </form>
  </div>
</section>
<style>
  .contact-form {
    display: grid;
    grid-template-columns: 1fr 1fr;
    padding: 60px;
    margin: 60px;
  }

  .kontakt-tekst {
    margin: 20px;
    padding: 20px;
  }
  #responseMessage {
    margin-top: 15px;
    color: #28a745;
    font-size: 8px;
  }

  form {
    max-width: 600px;
    margin: 0 auto;
  }

  label {
    display: block;
    font-family: adobe-caslon-pro, serif;
    font-style: normal;
    font-weight: 800;
    font-size: 18px;
    text-align: justify;
    color: #99040b;
    margin-bottom: 0px;
  }

  input {
    width: 100%;
    padding: 8px;
    margin-bottom: 25px;
    border: 1px solid #ccc;
    border-radius: 36px;
    border: 3px solid #c3e4dd;
    margin-top: 0;
    height: 63px;
  }

  textarea {
    width: 100%;
    padding: 30px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 36px;
    font-size: 15px;
    border: 3px solid #c3e4dd;
    margin-top: 1px;
  }

  button {
    background-color: #99040b;
    color: #c3e4dd;
    border: 0.5px solid #c3e4dd;
    width: fit-content;
    height: 63px;
    padding: 0.7rem 2rem;
    border-radius: 36px;
    font-size: 24px;
    font-family: adobe-caslon-pro, serif;
    font-style: normal;
    font-weight: 400;
    transition: transform 0.3s ease;
  }

  button:hover {
    transform: scale(1.1); /* Slightly larger */
  }

  /* Responsive Design - Mobile First */
  @media (max-width: 768px) {
    .contact-form {
      grid-template-columns: 1fr; /* Stack the form elements on top of each other */
      padding: 20px;
      margin: 20px;
    }

    .kontakt-tekst {
      margin: 15px;
      padding: 15px;
    }

    .kontakt-tekst p {
      font-size: 24px; /* Adjust text size for smaller screens */
      margin-bottom: 10px;
      word-spacing: 0; /* Reduce space between words */
    }

    label {
      font-size: 16px; /* Smaller font size for labels */
      margin-bottom: 2px;
    }

    input,
    textarea {
      padding: 12px; /* Increase padding for easier input on mobile */
      font-size: 16px; /* Larger font size for readability */
      margin-top: 0;
    }

    button {
  
      font-size: 18px;
      padding: 0.5rem 1.5rem;
      height: auto;
    }
  }    
  
</style>
